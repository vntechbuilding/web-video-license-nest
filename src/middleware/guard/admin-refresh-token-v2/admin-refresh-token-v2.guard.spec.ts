import { AdminRefreshTokenV2Guard } from './admin-refresh-token-v2.guard';

describe('AdminRefreshTokenV2Guard', () => {
  it('should be defined', () => {
    expect(new AdminRefreshTokenV2Guard()).toBeDefined();
  });
});
